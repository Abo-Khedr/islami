import 'package:flutter/material.dart';

class QurranTab extends StatelessWidget {
  const QurranTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
